import { Metadata } from "next";

import { plusJakartaSansFont, robotoMono } from "@/app/fonts";
import Navbar from "@/components/Navbar";
import { SocketProvider } from "@/lib/socket-context";

import "./globals.css";

export const metadata: Metadata = {
  title: "Gamba App - Crash Game",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${plusJakartaSansFont.variable} ${robotoMono.variable} antialiased`}
      >
        <SocketProvider>
          <div className="font-main bg-background-200 text-white">
            <Navbar />
            {children}
          </div>
        </SocketProvider>
      </body>
    </html>
  );
}
